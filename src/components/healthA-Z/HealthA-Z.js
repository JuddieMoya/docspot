import React from "react";
import { useSelector, useDispatch } from "react-redux";
import "./Health.css"

export const Health = () => {
    return (
        <ReactComponent>
            <h1>What is your offliction?</h1>
        </ReactComponent>
    )
}